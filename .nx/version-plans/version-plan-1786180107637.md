---
core-bundle: minor
---

refactor(core)!: 从基类提取了歌词行后处理逻辑

`setOptimizeOptions` 的行为已发生变化，现在将会自动重建歌词行数组和刷新视图，不再需要手动重新设置歌词行数组

提供了一个新的 `updateLyricProcessConfig` 和 `setLyricProcessConfig` API 用于批量设置歌词行优化选项和掩码设置以减小刷新开销
